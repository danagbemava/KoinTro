<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nexus.kointro" >

    <application
        android:name=".config.KoinApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.KoinTro" >
        <activity
            android:name=".core_features.todo_list.TodoListScreen"
            android:exported="false"
            android:label="@string/title_activity_todo_list_screen"
            android:theme="@style/Theme.KoinTro" />
        <activity
            android:name=".core_features.ui.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.KoinTro" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>